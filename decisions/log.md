<!-- Language rule: all file content in English. User communicates in Spanish; reply in Spanish, write files in English. -->
# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-07-27] DECISION: Course for friends will cover Claude Code + Automation only (not the full curso-docs vault), targeting 6-10 friends, leaning toward a live hands-on workshop format. | REASONING: This scope overlaps directly with what Krato sells, so it doubles as a pilot for a future paid product and a source of case studies/testimonials — a live, build-something-real format serves the favor/pilot/testimonial goals simultaneously better than recorded content. | CONTEXT: No start date set yet. Source material is `curso-docs/wiki/` (Tribu Divisual knowledge base). See `projects/course-for-friends/README.md`.

[2026-07-27] DECISION: Cut the curriculum from 7 to 6 sessions — dropped Apify from Session 4, dropped "how to sell agentic workflows" from Session 5, and removed the standalone "Deploy to production" session (Trigger.dev/Modal/security review) entirely. Scheduled Tasks/Loops survived by folding into the Capstone's Fase 3 instead. | REASONING: Antonio's call to tighten scope — no reasoning given beyond wanting a leaner curriculum; the cloud-deploy pipeline was judged out of scope for a friends course. | CONTEXT: Each friend works on their own real project across all 6 sessions (not a shared practice case). Sessions 1-3 are fully scripted point-by-point in `projects/course-for-friends/sessions/`; 4-6 are outline-only, pending detailed drafting.

[2026-08-04] DECISION: Validated a Higgsfield-MCP pipeline (photos → AI video walkthrough) as the production method for the `airbnb-video-leads` offer, using `cinematic_studio_video_v2` at 4s/clip, 9:16, ~4 credits/clip. | REASONING: Cheapest model that still handled the horizontal-photo→vertical-video reframe well natively (no need for the separate `reframe` tool, which alone would have cost more than the entire 9-clip video); Higgsfield has no generic music model, so background music is sourced from Pixabay's royalty-free catalog and mixed in locally with ffmpeg. | CONTEXT: Demo built from 9 photos of a CDMX studio listing in `/Users/antoniocain/Downloads/RECORRIDO`, output at `projects/airbnb-video-leads/demo-video/recorrido-demo-cdmx.mp4`. Full pipeline notes in that project's README for reuse on future leads.

[2026-08-05] DECISION: The Airbnb video-walkthrough offer is formalized as "Krato Recorridos" — a sub-brand of Project Krato, not a separate legal entity, reusing the projectkrato.com domain. It's modeled inside the existing CRM BASE Airtable (new LEADS.`Servicio de interes` option "Recorridos en Video", no new table/base) rather than a standalone system. | REASONING: Antonio confirmed it must stay a sub-brand specifically to reuse the domain; reusing the existing CRM LEADS/FUENTES structure avoids duplicating pipeline plumbing for a one-person, single-CRM operation. | CONTEXT: 53 of the leads sourced in `leads.csv` (verified emails) were imported into LEADS, linked to the existing FUENTES record "Scraper Claude Code / Organico". The 61 no-email leads in `leads-no-email.csv` were left out of the CRM pending a workable contact channel. See `projects/airbnb-video-leads/README.md`.

[2026-08-05] DECISION: Krato Recorridos gets its own visual identity — warm plaster/terracotta/rosa-mexicano/añil palette (Barragán-inspired, CDMX-grounded) with Fraunces serif + Instrument Sans, distinct from Project Krato's black/electric-lime/Trebuchet-MS tech-startup system. | REASONING: Antonio's explicit correction after seeing the first draft (which reused Project Krato's exact design system) — he wants the sub-brand to feel "envolvente" and "de clase," not technological, and to read as genuinely its own brand rather than a reskinned Project Krato page. | CONTEXT: `recorridos.html` and `gracias-recorridos.html` in `codigo krato/workspace`. The funnel strategy (bridge section pointing to Project Krato, same CRM/domain) is unchanged — only the visual identity moved. Project Krato connection is now revealed contextually (bridge section, quiet footer credit) instead of a matching nav badge on every load.

[2026-08-05] DECISION: Krato Recorridos was reskinned a second time to adopt Airbnb's actual design system (white canvas, ink #222 text, single Rausch #ff385c accent, Inter type, soft 8px/14px rounding, one minimal shadow tier) — replacing the Barragán-inspired multi-color hero from the same day. | REASONING: Antonio provided a full extracted Airbnb design-system doc and chose the full reskin over a partial recolor, reasoning that the audience (Airbnb hosts) will read the page as more native/familiar if it borrows Airbnb's own visual language, rather than an unrelated original palette. The multi-plane color hero was dropped entirely — it conflicted with Airbnb's actual "single accent on white" discipline, so a like-for-like hex swap wasn't faithful to the reference. | CONTEXT: New signature element is a recreation of Airbnb's pill search bar ("pega el link de tu Airbnb" + circular Rausch orb) as the hero hook — it doesn't submit separately, it scrolls to and prefills the real Netlify form to avoid two parallel capture paths. Reference doc: `Designs/Airbnb/DESIGN.md` (outside this repo, Antonio's Desktop). Files: `recorridos.html`, `gracias-recorridos.html` in `codigo krato/workspace`.

[2026-08-05] DECISION: Krato Recorridos leads moved from the shared LEADS table into a new dedicated `RECORRIDOS - LEADS` Airtable table, with the listing link split into its own `url` field instead of being embedded in free text. | REASONING: The first real form submission exposed the gap — LEADS has no field for a listing link or property count, so that data landed unreadable inside a generic Notas blob. Antonio asked directly for "control" over these leads; a dedicated table with structured fields (Link de Airbnb as url, Cuantas propiedades as a select, its own Estado pipeline: Lead nuevo → Contactado → Video en producción → Video entregado → Cliente) solves that, while still linking to the shared FUENTES table so conversion tracking stays unified. | CONTEXT: Migrated all 53 CSV-sourced leads (recovering the Airbnb link from the original `leads.csv`, not present in the old LEADS import) plus real webhook-sourced leads; the old LEADS copies were deleted to avoid duplication. n8n workflow `xFRLOJF4A3QK57nm` and the HTML form (`airbnb_link` + `notas` as separate fields, was one combined `mensaje` textarea) were updated to match. See `projects/airbnb-video-leads/README.md`.

[2026-08-05] DECISION: n8n's Respond-to-Webhook redirect after the Recorridos form submission must use HTTP 303, not the default 307. | REASONING: 307 preserves the original request method per spec, so the browser re-POSTs to the static `gracias-recorridos.html` page after the redirect — this is what caused Safari to report "la dirección no es válida" on a real test submission. 303 forces the follow-up request to downgrade to GET (the standard Post/Redirect/Get pattern). | CONTEXT: Fixed via `n8n_update_partial_workflow` on all three Respond-to-Webhook nodes in workflow `xFRLOJF4A3QK57nm`; verified with `curl -L` (without forcing `-X POST`, so curl's redirect-following matches real browser behavior) that the chain now lands on a clean `200`.

[2026-08-05] DECISION: Krato Recorridos portfolio pricing set at $90 USD/video (1-4 properties), $70 USD/video (5-9), $55 USD/video (10+) — one flat rate per tier, no separate premium tier for the amenities-showcase treatment (unit + full building amenities, as delivered in the Joseph demo). | REASONING: Antonio's call, prioritizing a simple volume table over standard/premium tiering — the more elaborate showcase style is included at the flat rate rather than upcharged. | CONTEXT: First real portfolio quote, going to Joseph (10+ CDMX properties, landing-page-organic lead, still "Lead nuevo" in `RECORRIDOS - LEADS`). Initially flagged a possible clash with the "Desde $70 USD" line from the email-outreach copy — turned out to be a non-issue: the live landing page already runs the exact matching 3-tier structure (Individual $90/1 video, Paquete de 5 $350 = $70 c/u "más popular", Portafolio 6+ "a medida, cotizar") confirmed by Antonio 2026-08-05. Joseph's 10+ falls cleanly into the "a medida" bucket at the agreed $55/video. Remaining loose end: the 25 cold emails already sent (batches 1-3, 2026-08-05) say "Desde $70 USD por video" without the tier context, which could set a wrong expectation if a 1-2-property lead from that batch replies expecting $70 instead of the real $90 individual rate — unresolved, Antonio to decide if/when it comes up.

[2026-08-02] DECISION: Client site saulsuli.com stays on Netlify (Personal plan, $9/mo) instead of migrating to Vercel. | REASONING: Vercel's free Hobby tier is explicitly non-commercial-use only per its own FAQ — a client production site would require Pro at $20/mo, more than double Netlify's $9/mo Personal plan for the same static-hosting result. | CONTEXT: Triggered by a Netlify billing/plan issue on saulsuli.com; resolved by paying for Netlify Personal. A test deploy was created on Vercel (`project-krato/saul-suli-web`, GitHub-connected, live at saul-suli-web.vercel.app) before the cost comparison — it's dormant/free and can be deleted or left as a cold backup. Repo: `tonocain/saul-suli-web`. DNS for saulsuli.com was never touched.
