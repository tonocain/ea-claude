# Project Krato — Website

**Status:** Live, active.

**Local code path:** `/Users/antoniocain/Desktop/PROJECT KRATO/codigo krato/workspace`
(the only active copy — older duplicate/version folders live in `codigo krato/archive/` for reference)

**GitHub:** `tonocain/codigo-krato` (public), branch `main`.

**Hosting/deploy:** Netlify, site id `fbc68c59-7f1e-4594-9f2d-58438a0e64dd`, linked via Netlify CLI (`.netlify/` in the local folder). Auto-deploys on push to `main` — no manual deploy step needed.

**Stack:** static site (`INDEX.html`, `gracias.html`, `favicon.svg`, `og-image.jpg`, `sitemap.xml`, `robots.txt`).

## Workflow

To make changes:
1. Work in `codigo krato/workspace` (not the archived folders).
2. Commit and push to `main` on GitHub.
3. Netlify picks up the push and rebuilds automatically.

This is a separate git repo from EA-CLAUDE — not tracked inside this repo. This README is just a pointer so future sessions know where the code lives and how deploy works.
