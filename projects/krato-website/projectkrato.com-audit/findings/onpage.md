# On-Page SEO — projectkrato.com

Score: 72/100

## What works

- Exactly one `<h1>`, followed by a clean `h2`/`h3` hierarchy (4 × h2, 7 × h3) with no skipped levels.
- Complete Open Graph + Twitter Card tags, correct `og:image` dimensions declared (1200×630).
- `<meta name="viewport">` present; canonical present.

## Findings

| Severity | Finding | Recommendation |
|---|---|---|
| Medium | Title tag is ~71 characters: "Project Krato \| Sistemas de Venta con IA + Software a Medida \| México" — likely to truncate in the SERP past ~60-70 chars. | Trim to something like "Project Krato — Sistemas de Venta con IA para PYMEs México" (~58 chars). |
| Medium | Meta description is ~165 characters, over the ~155-160 char comfort zone. | Tighten to the strongest single sentence + CTA, under 155 chars. |
| Medium | Keyword targeting is spread thin across one page for several distinct commercial terms (see `content.md` — same root cause). | Addressed via content restructuring, not an on-page tag fix. |
| Low | Internal linking is limited to in-page anchors (`#servicios`, `#metodo`, `#contacto`) — no additional indexable pages to link between yet besides the new `aviso-legal.html`. | Will resolve naturally as more pages are added; for now, confirm the footer link to `aviso-legal.html` is present sitewide (it is, as of this audit). |
